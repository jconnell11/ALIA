// Category definitions and rules for SceneVis kernel
// ========================================================

RULE 1 - "Far is a distance"
    if:  hq-1 -lex-  far
              -hq--> obj-1
  then: ako-1 -lex-  distance
              -ako-> hq-1

RULE 2 - "A farness is a distance"
    if: ako-1 -lex-  farness
              -ako-> hq-1
  then: ako-2 -lex-  distance
              -ako-> hq-1

RULE 3 - "A distance is a farness"
    if: ako-1 -lex-  distance
              -ako-> hq-1
  then: ako-2 -lex-  farness
              -ako-> hq-1

RULE 4 - "Medium close is a distance"
    if:  hq-1 -lex-  medium close
              -hq--> obj-1
  then: ako-1 -lex-  distance
              -ako-> hq-1

RULE 5 - "Close is a distance"
    if:  hq-1 -lex-  close
              -hq--> obj-1
  then: ako-1 -lex-  distance
              -ako-> hq-1

RULE 6 - "A closeness is a distance"
    if: ako-1 -lex-  closeness
              -ako-> hq-1
  then: ako-2 -lex-  distance
              -ako-> hq-1

RULE 7 - "A distance is a closeness"
    if: ako-1 -lex-  distance
              -ako-> hq-1
  then: ako-2 -lex-  closeness
              -ako-> hq-1

// ------------------------------------------------

RULE 8 - "Small is a size"
    if:  hq-1 -lex-  small
              -hq--> obj-1
  then: ako-1 -lex-  size
              -ako-> hq-1

RULE 9 - "A smallness is a size"
    if: ako-1 -lex-  smallness
              -ako-> hq-1
  then: ako-2 -lex-  size
              -ako-> hq-1

RULE 10 - "A size is a smallness"
    if: ako-1 -lex-  size
              -ako-> hq-1
  then: ako-2 -lex-  smallness
              -ako-> hq-1

RULE 11 - "Medium big is a size"
    if:  hq-1 -lex-  medium big
              -hq--> obj-1
  then: ako-1 -lex-  size
              -ako-> hq-1

RULE 12 - "Big is a size"
    if:  hq-1 -lex-  big
              -hq--> obj-1
  then: ako-1 -lex-  size
              -ako-> hq-1

RULE 13 - "A bigness is a size"
    if: ako-1 -lex-  bigness
              -ako-> hq-1
  then: ako-2 -lex-  size
              -ako-> hq-1

RULE 14 - "A size is a bigness"
    if: ako-1 -lex-  size
              -ako-> hq-1
  then: ako-2 -lex-  bigness
              -ako-> hq-1

// ------------------------------------------------

RULE 15 - "Compact is a length"
    if:  hq-1 -lex-  compact
              -hq--> obj-1
  then: ako-1 -lex-  length
              -ako-> hq-1

RULE 16 - "A compactness is a length"
    if: ako-1 -lex-  compactness
              -ako-> hq-1
  then: ako-2 -lex-  length
              -ako-> hq-1

RULE 17 - "A length is a compactness"
    if: ako-1 -lex-  length
              -ako-> hq-1
  then: ako-2 -lex-  compactness
              -ako-> hq-1

RULE 18 - "Medium long is a length"
    if:  hq-1 -lex-  medium long
              -hq--> obj-1
  then: ako-1 -lex-  length
              -ako-> hq-1

RULE 19 - "Long is a length"
    if:  hq-1 -lex-  long
              -hq--> obj-1
  then: ako-1 -lex-  length
              -ako-> hq-1

RULE 20 - "A longness is a length"
    if: ako-1 -lex-  longness
              -ako-> hq-1
  then: ako-2 -lex-  length
              -ako-> hq-1

RULE 21 - "A length is a longness"
    if: ako-1 -lex-  length
              -ako-> hq-1
  then: ako-2 -lex-  longness
              -ako-> hq-1

// ------------------------------------------------

RULE 22 - "Narrow is a width"
    if:  hq-1 -lex-  narrow
              -hq--> obj-1
  then: ako-1 -lex-  width
              -ako-> hq-1

RULE 23 - "A narrowness is a width"
    if: ako-1 -lex-  narrowness
              -ako-> hq-1
  then: ako-2 -lex-  width
              -ako-> hq-1

RULE 24 - "A width is a narrowness"
    if: ako-1 -lex-  width
              -ako-> hq-1
  then: ako-2 -lex-  narrowness
              -ako-> hq-1

RULE 25 - "Medium wide is a width"
    if:  hq-1 -lex-  medium wide
              -hq--> obj-1
  then: ako-1 -lex-  width
              -ako-> hq-1

RULE 26 - "Wide is a width"
    if:  hq-1 -lex-  wide
              -hq--> obj-1
  then: ako-1 -lex-  width
              -ako-> hq-1

RULE 27 - "A wideness is a width"
    if: ako-1 -lex-  wideness
              -ako-> hq-1
  then: ako-2 -lex-  width
              -ako-> hq-1

RULE 28 - "A width is a wideness"
    if: ako-1 -lex-  width
              -ako-> hq-1
  then: ako-2 -lex-  wideness
              -ako-> hq-1

// ------------------------------------------------

RULE 29 - "Thin is a thickness"
    if:  hq-1 -lex-  thin
              -hq--> obj-1
  then: ako-1 -lex-  thickness
              -ako-> hq-1

RULE 30 - "A thinness is a thickness"
    if: ako-1 -lex-  thinness
              -ako-> hq-1
  then: ako-2 -lex-  thickness
              -ako-> hq-1

RULE 31 - "A thickness is a thinness"
    if: ako-1 -lex-  thickness
              -ako-> hq-1
  then: ako-2 -lex-  thinness
              -ako-> hq-1

RULE 32 - "Medium thick is a thickness"
    if:  hq-1 -lex-  medium thick
              -hq--> obj-1
  then: ako-1 -lex-  thickness
              -ako-> hq-1

RULE 33 - "Thick is a thickness"
    if:  hq-1 -lex-  thick
              -hq--> obj-1
  then: ako-1 -lex-  thickness
              -ako-> hq-1

// ------------------------------------------------

RULE 34 - "Short is a height"
    if:  hq-1 -lex-  short
              -hq--> obj-1
  then: ako-1 -lex-  height
              -ako-> hq-1

RULE 35 - "A shortness is a height"
    if: ako-1 -lex-  shortness
              -ako-> hq-1
  then: ako-2 -lex-  height
              -ako-> hq-1

RULE 36 - "A height is a shortness"
    if: ako-1 -lex-  height
              -ako-> hq-1
  then: ako-2 -lex-  shortness
              -ako-> hq-1

RULE 37 - "Medium tall is a height"
    if:  hq-1 -lex-  medium tall
              -hq--> obj-1
  then: ako-1 -lex-  height
              -ako-> hq-1

RULE 38 - "Tall is a height"
    if:  hq-1 -lex-  tall
              -hq--> obj-1
  then: ako-1 -lex-  height
              -ako-> hq-1

RULE 39 - "A tallness is a height"
    if: ako-1 -lex-  tallness
              -ako-> hq-1
  then: ako-2 -lex-  height
              -ako-> hq-1

RULE 40 - "A height is a tallness"
    if: ako-1 -lex-  height
              -ako-> hq-1
  then: ako-2 -lex-  tallness
              -ako-> hq-1

// ------------------------------------------------

RULE 41 - "Red is a color"
    if:  hq-1 -lex-  red
              -hq--> obj-1
  then: ako-1 -lex-  color
              -ako-> hq-1

RULE 42 - "Orange is a color"
    if:  hq-1 -lex-  orange
              -hq--> obj-1
  then: ako-1 -lex-  color
              -ako-> hq-1

RULE 43 - "Yellow is a color"
    if:  hq-1 -lex-  yellow
              -hq--> obj-1
  then: ako-1 -lex-  color
              -ako-> hq-1

RULE 44 - "Green is a color"
    if:  hq-1 -lex-  green
              -hq--> obj-1
  then: ako-1 -lex-  color
              -ako-> hq-1

RULE 45 - "Blue is a color"
    if:  hq-1 -lex-  blue
              -hq--> obj-1
  then: ako-1 -lex-  color
              -ako-> hq-1

RULE 46 - "Purple is a color"
    if:  hq-1 -lex-  purple
              -hq--> obj-1
  then: ako-1 -lex-  color
              -ako-> hq-1

RULE 47 - "Black is a color"
    if:  hq-1 -lex-  black
              -hq--> obj-1
  then: ako-1 -lex-  color
              -ako-> hq-1

RULE 48 - "Gray is a color"
    if:  hq-1 -lex-  gray
              -hq--> obj-1
  then: ako-1 -lex-  color
              -ako-> hq-1

RULE 49 - "White is a color"
    if:  hq-1 -lex-  white
              -hq--> obj-1
  then: ako-1 -lex-  color
              -ako-> hq-1

// ++++++++++++++++++++++++++++++++++++++++++++++++++++++++

RULE 50 - "A distance is a measurement"
    if: ako-1 -lex-  distance
              -ako-> hq-1
  then: ako-2 -lex-  measurement
              -ako-> hq-1

RULE 51 - "A size is a measurement"
    if: ako-1 -lex-  size
              -ako-> hq-1
  then: ako-2 -lex-  measurement
              -ako-> hq-1

RULE 52 - "A length is a measurement"
    if: ako-1 -lex-  length
              -ako-> hq-1
  then: ako-2 -lex-  measurement
              -ako-> hq-1

RULE 53 - "A width is a measurement"
    if: ako-1 -lex-  width
              -ako-> hq-1
  then: ako-2 -lex-  measurement
              -ako-> hq-1

RULE 54 - "A thickness is a measurement"
    if: ako-1 -lex-  thickness
              -ako-> hq-1
  then: ako-2 -lex-  measurement
              -ako-> hq-1

RULE 55 - "A height is a measurement"
    if: ako-1 -lex-  height
              -ako-> hq-1
  then: ako-2 -lex-  measurement
              -ako-> hq-1

// ------------------------------------------------

RULE 56 - "Leftmost is a measurement"
    if:  hq-1 -lex-  leftmost
              -hq--> obj-1
  then: ako-2 -lex-  measurement
              -ako-> hq-1

RULE 57 - "Rightmost is a measurement"
    if:  hq-1 -lex-  rightmost
              -hq--> obj-1
  then: ako-2 -lex-  measurement
              -ako-> hq-1

RULE 58 - "Middle is a measurement"
    if:  hq-1 -lex-  middle
              -hq--> obj-1
  then: ako-2 -lex-  measurement
              -ako-> hq-1

// ------------------------------------------------

RULE 59 - "If something is visible then you see it"
    if:  hq-2 -lex-  visible
              -hq--> obj-1
  then: act-3 -lex-  see
              -tag-  vimp
              -obj-> obj-1

RULE 60 - "If you see something then it is visible"
    if: act-1 -lex-  see
              -tag-  vimp
              -obj-> obj-2
  then:  hq-3 -lex-  visible
              -hq--> obj-2



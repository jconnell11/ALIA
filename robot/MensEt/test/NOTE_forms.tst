if something is close drive backward                           // <situation> <cmd> <$proc>
unless you are tired if something is close spin left           // <situation> <cmd> <$proc> with unless
if something is close but it is not red then toot              // <situation> <cmd> <$proc> with unless
if something red is close then step forward                    // <situation> <cmd> <$proc> with two conds
if something is orange and it is striped squawk                // <situation> <cmd> <$proc> with two conds

if something is close drive backward then step forward         // <situation> <cmd> <seq> <cmd>
if something is close bleep and turn left                      // <situation> <%play>
if something is close drive backward then bleep and turn left  // <situation> <cmd> <seq> <%play>

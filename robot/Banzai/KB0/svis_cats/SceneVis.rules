// Standard inferences for "SceneVis" kernel
// ================================================

RULE 1 - close is a distance
    if:  hq-1 -lex-  close
  then: ako-1 -lex-  distance
              -ako-> hq-1

RULE 2 - a closeness is a distance
    if: ako-1 -lex-  closeness
              -ako-> hq-1
  then: ako-2 -lex-  distance
              -ako-> hq-1

RULE 3 - a distance is a closeness
    if: ako-1 -lex-  distance
              -ako-> hq-1
  then: ako-2 -lex-  closeness
              -ako-> hq-1

RULE 4 - medium close is a distance
    if:  hq-1 -lex-  medium close
  then: ako-1 -lex-  distance
              -ako-> hq-1

RULE 5 - far is a distance
    if:  hq-1 -lex-  far
  then: ako-1 -lex-  distance
              -ako-> hq-1

RULE 6 - a farness is a distance
    if: ako-1 -lex-  farness
              -ako-> hq-1
  then: ako-2 -lex-  distance
              -ako-> hq-1

RULE 7 - a distance is a farness
    if: ako-1 -lex-  distance
              -ako-> hq-1
  then: ako-2 -lex-  farness
              -ako-> hq-1

// ================================================

RULE 8 - small is a size
    if:  hq-1 -lex-  small
  then: ako-1 -lex-  size
              -ako-> hq-1

RULE 9 - a smallness is a size
    if: ako-1 -lex-  smallness
              -ako-> hq-1
  then: ako-2 -lex-  size
              -ako-> hq-1

RULE 10 - a size is a smallness
    if: ako-1 -lex-  size
              -ako-> hq-1
  then: ako-2 -lex-  smallness
              -ako-> hq-1

RULE 11 - medium big is a size
    if:  hq-1 -lex-  medium big
  then: ako-1 -lex-  size
              -ako-> hq-1

RULE 12 - big is a size
    if:  hq-1 -lex-  big
  then: ako-1 -lex-  size
              -ako-> hq-1

RULE 13 - a bigness is a size
    if: ako-1 -lex-  bigness
              -ako-> hq-1
  then: ako-2 -lex-  size
              -ako-> hq-1

RULE 14 - a size is a bigness
    if: ako-1 -lex-  size
              -ako-> hq-1
  then: ako-2 -lex-  bigness
              -ako-> hq-1

// ================================================

RULE 15 - compact is a length
    if:  hq-1 -lex-  compact
  then: ako-1 -lex-  length
              -ako-> hq-1

RULE 16 - a compactness is a length
    if: ako-1 -lex-  compactness
              -ako-> hq-1
  then: ako-2 -lex-  length
              -ako-> hq-1

RULE 17 - a length is a compactness
    if: ako-1 -lex-  length
              -ako-> hq-1
  then: ako-2 -lex-  compactness
              -ako-> hq-1

RULE 18 - medium long is a length
    if:  hq-1 -lex-  medium long
  then: ako-1 -lex-  length
              -ako-> hq-1

RULE 19 - long is a length
    if:  hq-1 -lex-  long
  then: ako-1 -lex-  length
              -ako-> hq-1

RULE 20 - a longness is a length
    if: ako-1 -lex-  longness
              -ako-> hq-1
  then: ako-2 -lex-  length
              -ako-> hq-1

RULE 21 - a length is a longness
    if: ako-1 -lex-  length
              -ako-> hq-1
  then: ako-2 -lex-  longness
              -ako-> hq-1

// ================================================

RULE 22 - narrow is a width
    if:  hq-1 -lex-  narrow
  then: ako-1 -lex-  width
              -ako-> hq-1

RULE 23 - a narrowness is a width
    if: ako-1 -lex-  narrowness
              -ako-> hq-1
  then: ako-2 -lex-  width
              -ako-> hq-1

RULE 24 - a width is a narrowness
    if: ako-1 -lex-  width
              -ako-> hq-1
  then: ako-2 -lex-  narrowness
              -ako-> hq-1

RULE 25 - medium wide is a width
    if:  hq-1 -lex-  medium wide
  then: ako-1 -lex-  width
              -ako-> hq-1

RULE 26 - wide is a width
    if:  hq-1 -lex-  wide
  then: ako-1 -lex-  width
              -ako-> hq-1

RULE 27 - a wideness is a width
    if: ako-1 -lex-  wideness
              -ako-> hq-1
  then: ako-2 -lex-  width
              -ako-> hq-1

RULE 28 - a width is a wideness
    if: ako-1 -lex-  width
              -ako-> hq-1
  then: ako-2 -lex-  wideness
              -ako-> hq-1

// ================================================

RULE 29 - thin is a thickness
    if:  hq-1 -lex-  thin
  then: ako-1 -lex-  thickness
              -ako-> hq-1

RULE 30 - a thinness is a thickness
    if: ako-1 -lex-  thinness
              -ako-> hq-1
  then: ako-2 -lex-  thickness
              -ako-> hq-1

RULE 31 - a thickness is a thinness
    if: ako-1 -lex-  thickness
              -ako-> hq-1
  then: ako-2 -lex-  thinness
              -ako-> hq-1

RULE 32 - medium thick is a thickness
    if:  hq-1 -lex-  medium thick
  then: ako-1 -lex-  thickness
              -ako-> hq-1

RULE 33 - thick is a thickness
    if:  hq-1 -lex-  thick
  then: ako-1 -lex-  thickness
              -ako-> hq-1

// ================================================

RULE 34 - short is a height
  if:  hq-1 -lex-  short
then: ako-1 -lex-  height
            -ako-> hq-1

RULE 35 - a shortness is a height
  if: ako-1 -lex-  shortness
            -ako-> hq-1
then: ako-2 -lex-  height
            -ako-> hq-1

RULE 36 - a height is a shortness
  if: ako-1 -lex-  height
            -ako-> hq-1
then: ako-2 -lex-  shortness
            -ako-> hq-1

RULE 37 - medium tall is a height
  if:  hq-1 -lex-  medium tall
then: ako-1 -lex-  height
            -ako-> hq-1

RULE 38 - tall is a height
  if:  hq-1 -lex-  tall
then: ako-1 -lex-  height
            -ako-> hq-1

RULE 39 - a tallness is a height
  if: ako-1 -lex-  tallness
            -ako-> hq-1
then: ako-2 -lex-  height
            -ako-> hq-1

RULE 40 - a height is a tallness
  if: ako-1 -lex-  height
            -ako-> hq-1
then: ako-2 -lex-  tallness
            -ako-> hq-1

// ================================================

RULE 41 - red is a color
  if:  hq-1 -lex-  red
then: ako-1 -lex-  color
            -ako-> hq-1

RULE 42 - a redness is a color
  if: ako-1 -lex-  redness
            -ako-> hq-1
then: ako-2 -lex-  color
            -ako-> hq-1

RULE 43 - a color is a redness
  if: ako-1 -lex-  color
            -ako-> hq-1
then: ako-2 -lex-  redness
            -ako-> hq-1

RULE 44 - orange is a color
  if:  hq-1 -lex-  orange
then: ako-1 -lex-  color
            -ako-> hq-1

RULE 45 - a orangeness is a color
  if: ako-1 -lex-  orangeness
            -ako-> hq-1
then: ako-2 -lex-  color
            -ako-> hq-1

RULE 46 - a color is a orangeness
  if: ako-1 -lex-  color
            -ako-> hq-1
then: ako-2 -lex-  orangeness
            -ako-> hq-1

RULE 47 - yellow is a color
  if:  hq-1 -lex-  yellow
then: ako-1 -lex-  color
            -ako-> hq-1

RULE 48 - a yellowness is a color
  if: ako-1 -lex-  yellowness
            -ako-> hq-1
then: ako-2 -lex-  color
            -ako-> hq-1

RULE 49 - a color is a yellowness
  if: ako-1 -lex-  color
            -ako-> hq-1
then: ako-2 -lex-  yellowness
            -ako-> hq-1

RULE 50 - green is a color
  if:  hq-1 -lex-  green
then: ako-1 -lex-  color
            -ako-> hq-1

RULE 51 - a greenness is a color
  if: ako-1 -lex-  greenness
            -ako-> hq-1
then: ako-2 -lex-  color
            -ako-> hq-1

RULE 52 - a color is a greenness
  if: ako-1 -lex-  color
            -ako-> hq-1
then: ako-2 -lex-  greenness
            -ako-> hq-1

RULE 53 - blue is a color
  if:  hq-1 -lex-  blue
then: ako-1 -lex-  color
            -ako-> hq-1

RULE 54 - a blueness is a color
  if: ako-1 -lex-  blueness
            -ako-> hq-1
then: ako-2 -lex-  color
            -ako-> hq-1

RULE 55 - a color is a blueness
  if: ako-1 -lex-  color
            -ako-> hq-1
then: ako-2 -lex-  blueness
            -ako-> hq-1

RULE 56 - purple is a color
  if:  hq-1 -lex-  purple
then: ako-1 -lex-  color
            -ako-> hq-1

RULE 57 - a purpleness is a color
  if: ako-1 -lex-  purpleness
            -ako-> hq-1
then: ako-2 -lex-  color
            -ako-> hq-1

RULE 58 - a color is a purpleness
  if: ako-1 -lex-  color
            -ako-> hq-1
then: ako-2 -lex-  purpleness
            -ako-> hq-1

// ------------------------------------------------

RULE 59 - black is a color
  if:  hq-1 -lex-  black
then: ako-1 -lex-  color
            -ako-> hq-1

RULE 60 - a blackness is a color
  if: ako-1 -lex-  blackness
            -ako-> hq-1
then: ako-2 -lex-  color
            -ako-> hq-1

RULE 61 - a color is a blackness
  if: ako-1 -lex-  color
            -ako-> hq-1
then: ako-2 -lex-  blackness
            -ako-> hq-1

RULE 62 - gray is a color
  if:  hq-1 -lex-  gray
then: ako-1 -lex-  color
            -ako-> hq-1

RULE 63 - a grayness is a color
  if: ako-1 -lex-  grayness
            -ako-> hq-1
then: ako-2 -lex-  color
            -ako-> hq-1

RULE 64 - a color is a grayness
  if: ako-1 -lex-  color
            -ako-> hq-1
then: ako-2 -lex-  grayness
            -ako-> hq-1

RULE 65 - white is a color
  if:  hq-1 -lex-  white
then: ako-1 -lex-  color
            -ako-> hq-1

RULE 66 - a whiteness is a color
  if: ako-1 -lex-  whiteness
            -ako-> hq-1
then: ako-2 -lex-  color
            -ako-> hq-1

RULE 67 - a color is a whiteness
  if: ako-1 -lex-  color
            -ako-> hq-1
then: ako-2 -lex-  whiteness
            -ako-> hq-1

// ================================================

RULE 68 - a distance is a measurement
  if: ako-1 -lex-  distance
            -ako-> hq-1
then: ako-2 -lex-  measurement
            -ako-> hq-1

RULE 69 - a size is a measurement
  if: ako-1 -lex-  size
            -ako-> hq-1
then: ako-2 -lex-  measurement
            -ako-> hq-1

RULE 70 - a length is a measurement
  if: ako-1 -lex-  length
            -ako-> hq-1
then: ako-2 -lex-  measurement
            -ako-> hq-1
       hq-2 -lex-  visual

RULE 71 - a width is a measurement
  if: ako-1 -lex-  width
            -ako-> hq-1
then: ako-2 -lex-  measurement
            -ako-> hq-1

RULE 72 - a thickness is a measurement
  if: ako-1 -lex-  thickness
            -ako-> hq-1
then: ako-2 -lex-  measurement
            -ako-> hq-1

RULE 73 - a height is a measurement
  if: ako-1 -lex-  height
            -ako-> hq-1
then: ako-2 -lex-  measurement
            -ako-> hq-1

// ================================================

RULE 74 - leftmost is a measurement
  if:  hq-1 -lex-  leftmost
            -hq--> obj-1
then: ako-2 -lex-  measurement
            -ako-> hq-1

RULE 75 - rightmost is a measurement
  if:  hq-1 -lex-  rightmost
            -hq--> obj-1
then: ako-2 -lex-  measurement
            -ako-> hq-1

RULE 76 - middle is a measurement
  if:  hq-1 -lex-  middle
            -hq--> obj-1
then: ako-2 -lex-  measurement
            -ako-> hq-1

// ================================================

RULE 77 - being next to is a visual relation
  if: loc-1 -lex-  next to
            -loc-> obj-1
then: ako-2 -lex-  relation
            -ako-> loc-1
       hq-2 -lex-  visual
            -hq--> loc-1

RULE 78 - being between is a visual relation
  if: loc-1 -lex-  between
            -loc-> obj-1
then: ako-2 -lex-  relation
            -ako-> loc-1
       hq-2 -lex-  visual
            -hq--> loc-1

// ================================================

RULE 79 - is something is to the left of something else it is left of it   
  if: loc-1 -lex-  to the left of
            -loc-> obj-1
            -wrt-> obj-2
then: loc-2 -lex-  left of
            -loc-> obj-1
            -wrt-> obj-2

RULE 80 - is something is to the right of something else it is right of it   
  if: loc-1 -lex-  to the right of
            -loc-> obj-1
            -wrt-> obj-2
then: loc-2 -lex-  right of
            -loc-> obj-1
            -wrt-> obj-2




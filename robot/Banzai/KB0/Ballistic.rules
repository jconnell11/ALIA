// Category definitions and rules for Ballistic kernel
// ========================================================

RULE 1 - "Robots have arms"
    if: ako-1 -lex-  robot
              -ako-> agt-1
  then: ako-2 -lex-  arm
              -ako-> obj-1
              -wrt-> agt-1

RULE 2 - "Robots have hands"
    if: ako-1 -lex-  robot
              -ako-> agt-1
  then: ako-2 -lex-  hand
              -ako-> obj-1
              -wrt-> agt-1

RULE 3 - "Robots have necks"
    if: ako-1 -lex-  robot
              -ako-> agt-1
  then: ako-2 -lex-  neck
              -ako-> obj-1
              -wrt-> agt-1

RULE 4 - "Robots have bodies"
    if: ako-1 -lex-  robot
              -ako-> agt-1
  then: ako-2 -lex-  body
              -ako-> obj-1
              -wrt-> agt-1

RULE 5 - "Robots have bases"
    if: ako-1 -lex-  robot
              -ako-> agt-1
  then: ako-2 -lex-  base
              -ako-> obj-1
              -wrt-> agt-1






